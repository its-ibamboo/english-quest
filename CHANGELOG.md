# 英文任務系統 · 更新紀錄

## v2026.09.03i

五個檔案要一起更新。

### 新增：每日挑戰

每天 10 題四選一，**全台灣同一天拿到的題目完全一樣**。

- **題目不需要後端**：用台北日期當種子從 `WORD_BANK` 產生，所以離線也能玩。只有「全站分佈」需要連線，連不上時照樣能作答，成績存在裝置上。
- **選項順序也固定**。只固定題目不固定選項的話，學生重新整理就能靠位置記憶作弊，而且看起來像壞掉。
- **結果頁**：分數、全台平均、你贏過幾 %、0–10 分的分佈長條（自己那格是金色）、答錯的字清單。
- **重玩不計入統計**。第一次的成績才送出，之後可以無限練習。
- **一人一天只計一次**：伺服器用 `daily_plays(day, aid)` 去重，`aid` 沿用既有的 `ibq_aid`，沒有新增任何識別資訊。該表只留最近 30 天，寫入時自動清掉更早的。
- **入口併在今日單字卡片裡**，不另外開一張卡、不佔九宮格位置，主頁不會變長。
- 難度目前是 basic + advanced（跟今日單字共用 `DAILY_LEVELS`）。之後要加地獄題，改那一行就好。

**副作用（好的那種）**：每個人玩一次就是 10 個字的曝光紀錄餵給 `word_stats`。100 人玩就是 1000 筆，會讓答錯率資料的累積速度快好幾倍——本來估兩週才到 100 次門檻，可能一週就到。

### 資料表
- `daily_challenge`：一天一列，`plays`、`total`、`s0`~`s10`。存分佈而不只存平均，才算得出「你贏過幾 %」。一年 365 列。
- `daily_plays`：去重用，只留 30 天。

### 修改
- `wrangler.toml` 的 `run_worker_first` 加入 `/d`。
- `sw.js` 的 BYPASS 加入 `/d`。
- 後台新增「每日挑戰」：今天玩過人數、平均分、參與率（玩過 ÷ 今天活躍人數），以及最近 14 天的歷史。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

新增的 localStorage key：`eq_news_seen`、`eq_daily`。

### 部署後檢查
1. `/ping` → 「版本是否一致：一致」
2. 主頁 → 今日單字卡下面有「⚡ 今日挑戰 · 10 題」
3. 玩完一輪 → 結果頁有分數與分佈圖；回主頁卡片變成「今日挑戰 8 / 10」
4. `/admin` → 「每日挑戰」區出現今天的人數
5. 重新整理 → 題目和選項順序都不會變
