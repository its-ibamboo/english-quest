# 英文任務系統 · 更新紀錄

## v2026.09.03n

五個檔案要一起更新。

### 修正

**一、回報表單的提示文字看起來像已經打好的字。**

兩個問題疊在一起：

- 結尾的省略號我寫成 `\u2026`，但那是**寫在 HTML 屬性裡**，HTML 不會解析跳脫序列，所以畫面上直接顯示出「`\u2026`」五個字元。改成真的「…」。
- `.rp-text` 指定了深色文字，但沒指定 `::placeholder` 的顏色，部分瀏覽器會讓提示文字沿用同一個深色，看起來就像使用者已經輸入過。補上 `::placeholder { color: #B5B8C4 }`。

順便掃過整份檔案，確認 HTML 區段沒有其他殘留的 `\uXXXX`（結果是 0 個）。

**二、`/admin` 上方加了「💬 問題回報」連結。**

不用再另外記 `/reports` 這個網址。連結下方註明「使用者送出的回報與截圖 · 需要密碼」，點進去仍然要輸入密碼。

### 承前版（v2026.09.03m）
站內問題回報表單、`/reports` 後台（密碼 `1234`）、自動夾帶診斷資料、可附截圖、限流與限大小。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

### 部署後檢查
1. `/ping` → 五項全部「有，已部署」
2. 頁尾「💬 問題回報」→ 表單裡的提示文字應該是**淺灰色**，而且結尾是「…」不是「\u2026」
3. `/admin` 最上方 → 有「💬 問題回報」連結，點進去要求輸入密碼
4. 送一筆測試回報 → 在 `/reports` 看得到，展開「診斷資料」有版本號和你的進度

### 建議
密碼 `1234` 寫在 `worker.js` 裡會進 GitHub。到 Cloudflare Dashboard → Settings → Variables 設一個 `ADMIN_KEY`，程式會優先用它。
