# 英文任務系統 · 更新紀錄

## v2026.09.03j

五個檔案要一起更新。這一版只修兩個問題，功能沒有新增。

### 修正

**一、`/ping` 誤報「複製偵測：沒有！部署的是舊版」。**

是 `/ping` 這支診斷自己寫錯，不是功能壞掉。它找的字串是 `/h?host=`，但程式碼裡實際寫的是：

```js
var REPORT = "https://" + HOME + "/h";
img.src = REPORT + "?host=" + ...
```

網址是組出來的，`/h?host=` 從來不會以完整字串出現在檔案裡，所以永遠判定為「沒有」。改成檢查 `var REPORT =`，並順便補上「每日挑戰」「答錯率追蹤」兩項的部署檢查。

複製偵測本身一直是正常的。

**二、每日挑戰顯示「你贏過 0% 的人」。**

第一個玩的人分數 8 分，卻看到「贏過 0%」——因為原本的算法只數「分數比你低的人」，自己不算在內，一個人玩的時候必然是 0。對第一個完成的人是打擊，也是誤導。

兩處修正：

- **改用中點百分位**：同分的人算一半。10 人裡 5 人低於你、4 人同分，就是 (5 + 2) ÷ 10 = 70%。
- **不滿 10 人不顯示排名**，改成「你是今天第 N 個完成的」，並註明「滿 10 人才會顯示排名」。樣本太少算百分位沒有意義。

### 承前版（v2026.09.03i）
每日挑戰、`daily_challenge` / `daily_plays` 資料表、後台每日挑戰區塊。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

### 部署後檢查
`/ping` 應該顯示：

```
版本是否一致：一致
埋點程式碼：有，已部署
複製偵測：有，已部署
每日挑戰：有，已部署
答錯率追蹤：有，已部署
```

五項都是「有」才算完整部署。
