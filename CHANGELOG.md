# 英文任務系統 · 更新紀錄

## v2026.09.03e

**五個檔案要一起更新**（多了 `sw.js`），缺一個會出問題。

### 新增
- **Service Worker（`sw.js`）**：離線也能用。策略是**網路優先**——有訊號永遠拿最新版，只有連不上時才用上次存下來的，不會讓學生卡在舊版本。`/t`、`/h`、`/admin`、`/ping` 一律不快取，統計與偵測維持實時。啟用時會自動刪掉舊版快取。
- **主頁**：預設落地頁改為主頁，模式排成 3 欄方塊。回主頁：分頁列「🏠 首頁」或點標題。
- **今日單字**：每天一個字，台北時間日期當種子，全部人同一天看到的都一樣。取材 basic + advanced 共 2047 字。
- **公告視窗**：右上「📢 最新消息」，有新公告顯示紅點並自動跳一次。
- **複製偵測**：檔案若被架在非官方網域，回報該網域（只送網域，不送使用者資料）。
- **版本號**：頁尾顯示版本，`/ping` 比對 index.html 與 worker.js 是否同版。

### 修正
- **公告不再外連 Threads。** 原本 Android / iPhone 兩則會把學生導到 Threads（需登入、有年齡限制）。改成把安裝步驟直接寫在公告裡，不用跳出去也不會過期。
- **「連對 / 答錯」只在單字測驗、文法、重組句子顯示。** 其他模式不會累加這兩個數字，原本一律顯示 0，看起來像壞掉。
- **分頁列改成單行橫向捲動。** 加入「首頁」後 8 項會折成三列參差不齊。現在可左右滑，切換後自動把目前分頁捲到中間。分頁列拿掉表情符號（只留「🏠 首頁」），主頁九宮格的圖示全部保留。

### 修改
- 排行榜佔位（灰色「敬請期待」）。
- 頁尾「問題回報」與「打賞箱」併成一行；「搜尋單字」與「最新消息」併成同一列。
- 統計埋點：進站預設模式由 `flash` 改為 `home`。
- `worker.js` 模式白名單加入 `home`（沒加則主頁事件被靜默丟棄）。
- `wrangler.toml` 的 `run_worker_first` 加入 `/h`（沒加則複製偵測 404）。
- 後台「每日」只顯示最近 14 天，其餘收進摺疊區。
- 後台「回訪」改成分組（1 / 2–3 / 4–7 / 8–14 / 15 天以上）並顯示百分比。
- 後台新增「⚠️ 偵測到的複製網域」，只有偵測到時才顯示。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK` 270 題、`SCRAMBLE_BANK` 183 句 — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同（已另行確認 2122 字無任何重複）
- 拼字小蜜蜂、徽章、打賞箱、發音修正表、搜尋、頁尾課綱標註

新增的 localStorage key 只有一把：`eq_news_seen`。

### 部署後檢查
1. `/ping` → 「版本是否一致：一致」、「複製偵測：有，已部署」
2. `/admin` → 資料還在
3. 用你自己的手機開首頁 → 闖關進度、徽章、拼字最佳成績都還在
4. **開啟後把飛航模式打開，重新開一次** → 應該還能用（第一次部署後要先正常開一次讓它存快取）

### 下一版預告
- 答錯率追蹤（新增 `word_stats` 表，批次上報，只存聚合不存明細）
- 同期群回訪（只看第一次來 7 天以上的人，排除新人稀釋）
- 最難單字榜 → 卡牌選字
- 排行榜
