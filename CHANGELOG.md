# 英文任務系統 · 更新紀錄

## v2026.09.03m

五個檔案要一起更新。

### 新增：站內問題回報

照麻將系統的做法。原本頁尾的「💬 問題回報」是連到 Threads，但大部分人不會為了回報一個 bug 特地跑去社群平台留言——而且那還會把國中生導出到社群平台。改成直接在系統裡填。

**真正的價值是自動夾帶診斷資料**：版本號、目前模式與難度、通關數、徽章數、最佳分數、今日挑戰狀態、匿名 ID、哪些紀錄存在、螢幕尺寸、語音支援、離線快取支援、連線狀態、瀏覽器 UA。這些是靠使用者自己描述拿不到的東西。

- **入口兩處**：頁尾「💬 問題回報」，以及公告視窗置頂「遇到問題？點這裡回報」（大部分人根本不知道可以回報）。
- **可附截圖**：前端先縮到 1000px 寬、JPEG 品質逐步降到 0.3，還是太大就放棄附圖。手機直出截圖動輒 3–5MB，不壓送不上去。
- **表單裡仍保留 Threads 連結**當備援。
- **明講「不會傳送你的姓名、學校或任何個人資料」**。
- 彈窗掛在 `#root` 之外，`render()` 重建畫面不會清掉。

### 後台：`/reports`（密碼 `1234`）

- **必須登入才看得到**。裡面是使用者打的字和他手機的截圖，不能像 `/admin` 那樣公開。
- 密碼優先讀 Cloudflare 環境變數 `ADMIN_KEY`，沒設才用程式裡的 `1234`。**建議之後到 Dashboard 設一個 `ADMIN_KEY`**，密碼就不會留在 repo 裡。
- 登入後存 30 天 cookie（HttpOnly、Secure、SameSite=Lax），不用每次輸入。
- 每筆可單獨刪除，也可以全部刪除（有二次確認）。
- 圖片用 `<details>` 收起來，要點才載入，列表不會因為十張圖變得又長又慢。
- 所有顯示的內容都經過 HTML 逃脫。

### 防濫用
- **限流**：同一個 IP 一分鐘最多 3 次。限流表超過 500 筆會清空，不會無限長大。
- **限大小**：整包超過 200KB 直接退回（前端縮圖是第一道，這是保險）。
- **只留最新 60 筆**，寫入時自動刪掉更舊的。圖片很佔空間。
- 圖片只接受 `data:image/` 開頭的字串。

### 修改
- `wrangler.toml` 的 `run_worker_first` 加入 `/report`、`/reports`、`/reports/*`。
- `sw.js` 的 BYPASS 加入 `/report`、`/reports`。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

回報模組完全獨立，不讀寫任何 `eq_` 開頭的 key。

### 部署後檢查
1. `/ping` → 五項全部「有，已部署」
2. 頁尾按「💬 問題回報」→ 表單跳出來
3. 隨便寫一句送出 → 顯示「已送出，謝謝你的回報！」
4. 開 `/reports` → 輸入 `1234` → 看得到剛剛那筆，展開「診斷資料」應該有版本號和你的進度
5. 按「刪除這筆」確認能刪掉
