# 英文任務系統 · 更新紀錄

## v2026.09.03p

五個檔案要一起更新。

### 新增：後台顯示未處理的回報筆數

照麻將系統的做法。`/admin` 最上方：

- **有回報時**：金框醒目卡片「📬 有 3 筆問題回報 / 點這裡查看與刪除 →」
- **刪光之後**：自動退成一行灰色小字「💬 問題回報 · 目前沒有新的回報 · 需要密碼」

沒有完全隱藏，是因為隱藏了就找不到入口、又得回去記 `/reports` 這個網址。

**只露出筆數，不露內容。** `/admin/data` 是公開的，所以那裡只回傳一個數字；要看使用者寫了什麼、附了什麼截圖，還是得帶密碼開 `/reports`。

### 修正
- CSS 裡的 `content: "\00B7"` 寫在 Worker 的樣板字串裡會被 JavaScript 當成八進位跳脫序列而語法錯誤，改用中點字元本身。

### 承前版
- v2026.09.03n：回報表單提示文字修正（`\u2026` 直接印出來、提示文字顏色太深）、`/admin` 加問題回報連結。
- v2026.09.03m：站內問題回報表單、`/reports` 後台（密碼 `1234`）、自動夾帶診斷資料。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

### 部署後檢查
1. `/ping` → 五項全部「有，已部署」
2. `/admin` 最上方 → 沒有回報時是一行灰色小字
3. 送一筆測試回報 → 重新整理 `/admin` → 變成金框「📬 有 1 筆問題回報」
4. 進 `/reports` 刪掉 → 重新整理 `/admin` → 又變回灰色小字
