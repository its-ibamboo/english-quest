# 英文任務系統 · 更新紀錄

## v2026.09.03k

五個檔案要一起更新。

### 修正：名次會一直往上跑

原本結果頁的「你是今天第 N 個完成的」是拿 `plays`（今天總共幾個人玩過）當名次。那是兩件不同的事——你第一個玩完時 `plays` 是 1，別人玩完之後變 2，你重開結果頁就變成「第 2 個」。今天有 300 人玩，你會變成「第 300 個完成的」，明明你是最早的那個。

改成：

- **伺服器在寫入那一筆的當下取名次**，回傳給前端。
- **前端把名次存進 `eq_daily`**，之後不管全站累積到幾人都不會變。
- **只寫入一次**，重新載入不會覆蓋。
- **重玩不顯示名次**（重玩本來就不計入統計）。
- **只在前 20 名內顯示**。「第 187 個完成的」聽起來像很慢，沒有必要講。
- **舊資料相容**：現有裝置的 `eq_daily` 沒有名次欄位，那種情況就不顯示名次，只顯示「今天有 N 個人玩過」。

### 公告
- 移除「徽章收集系統」那則。
- 新增「⚡ 新增今日挑戰：每天 10 題，全台灣同一份，答完看得到全台平均分」。

### 承前版
- v2026.09.03j：修正 `/ping` 誤報複製偵測、中點百分位、不滿 10 人不顯示排名。
- v2026.09.03i：每日挑戰。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

### 部署後檢查
1. `/ping` → 五項全部「有，已部署」
2. 主頁 → 公告裡有「新增今日挑戰」、沒有「徽章收集系統」
3. **玩完今日挑戰記下名次，請別人也玩一次，再回來看結果頁 → 名次不應該改變**（這是這一版修的東西）
