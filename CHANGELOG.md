# 英文任務系統 · 更新紀錄

## v2026.09.03h

五個檔案要一起更新。

### 新增：難度分佈（判斷使用者程度）

**沒有問使用者的年級**，理由：

- 自陳的身分準確度差 —— 學生沒有動機誠實回答
- 開場問問題會擋在漏斗前面，而「打開就走」正是目前最大的缺口
- 年級一年後就過期，今天填國二的人明年是國三
- 切分會讓 `word_stats` 的樣本量除以五。本來兩週能到 100 次門檻，變十週。這對最有價值的那份資料是直接傷害

改成看**學生實際選了哪個難度**：不打擾、不會過期、現在就收得到。

- 前端包裝 `setDifficulty`，切換難度時送一個 `diff` 事件；進站時也記一次預設值當基準線。
- 難度與模式共用 `mode` 欄位。既有查詢都有 `WHERE ev='mode'`，所以不會污染任何現有數字。
- 後台新增「程度 · 選了哪個難度」：只算**主動改過難度**的人（進站預設每個人都會送一次 `all`，混進來的話「全部」會永遠第一）。顯示人數、比例、切換次數。
- 附判讀說明：只停在基礎的偏國小或國中低年級；往進階跑的多半國二國三；碰地獄的是高中或程度特別好。搭配「整體答錯率」一起看更準。

**`word_stats` 維持全站聚合、不切分。** 那是商業資產，樣本量比細分重要。真要分年級的答錯率，是日活上千之後的事。

### 承前版
- v2026.09.03g：後台改版（每個數字有白話解釋、昨天對照、單位分區、誠實列出偏差），修好從來沒顯示過的長條圖。
- v2026.09.03f：單字答錯率追蹤、最常錯的字、同期群回訪。

### 沒有動到
- 學生所有紀錄：`eq_best`、`eq_mistakes`、`eq_stage_progress`、`eq_badges`、`eq_bee_best`
- `WORD_BANK` 2122 字（含順序）、`GRAMMAR_BANK`、`SCRAMBLE_BANK` — 逐字比對相同
- 107 個關卡內容 — 逐關逐字比對相同

### 部署後檢查
1. `/ping` → 「版本是否一致：一致」
2. `/admin` → 長條圖有顏色、卡片有白話解釋與「昨天 N」
3. 在首頁切一次難度（例如點「進階」），隔幾分鐘後 `/admin` 的「程度」區應該出現數字
